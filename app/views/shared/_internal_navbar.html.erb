<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
      >
      <i class="fas fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a><%= link_to "Merchants", merchants_path, class: "navbar-brand" %></a>
      </div>
      <div class="navbar-nav">
        <a><%= link_to "My Merchants", my_merchants_path, class: "navbar-brand" %></a>
      </div>
    </div>
    <% if current_user %>
      <div class="text-secondary"> Hello <%= current_user.email %></div>
      <%= link_to "Sing Out", destroy_user_session_path, methode: :delete, class: "btn btn-outline-secondary btn-sm" %>
    <% else %>
      <%= link_to "Sing Up", new_user_registration_path, class: "btn btn-outline-secondary btn-sm" %>
      <%= link_to "Sing In", new_user_session_path, class: "btn btn-outline-secondary btn-sm" %>
    <% end %>
  </div>
</nav>
<% if notice.present? %>
  <div class="notification is-link is-light"><%= notice %></div>
<% end %>

<% if alert.present? %>
  <div class="notification is-danger is-light"><%= alert %></div>
<% end %>
